# ============================================================
# 传统依赖清单（与 pyproject.toml 保持一致，二选一安装即可）
# 安装: pip install -r requirements.txt
# 说明: 契约包是纯声明性质的强类型代码，运行时仅依赖 Pydantic，
#       不含 Celery / 数据库 / LLM SDK 等任何业务实现依赖。
# ============================================================

# ---------- 核心运行时依赖 ----------
# 入参模型定义与校验
pydantic>=2.9,<3.0

# ---------- 开发 / 校验 / 构建（可选） ----------
pytest>=8.3
pytest-cov>=6.0
mypy>=1.14
ruff>=0.9
build>=1.2

# ---------- 文档（Read the Docs 构建使用） ----------
mkdocs-material>=9.5
mkdocstrings[python]>=0.27
